{
  "name": "rhys-beats",
  "version": "1.0.0",
  "description": "Beat vault for Cloudflare Pages",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy:manual": "wrangler pages deploy public",
    "db:apply:local": "wrangler d1 migrations apply rhys-beats --local",
    "db:apply:remote": "wrangler d1 migrations apply rhys-beats --remote"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250404.0",
    "wrangler": "^4.81.0"
  }
}
