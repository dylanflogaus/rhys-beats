# Beat Vault

Simple website to upload, preview, and manage beat files.

## Features

- Upload beat audio files with metadata (title, producer, BPM, key, notes)
- Stream beats in-browser
- Delete beats and their uploaded files
- SQLite storage for metadata

## Run locally

```bash
npm install
npm start
```

Open `http://localhost:3000`.
