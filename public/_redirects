# Avoid rewriting `/login` or `/register` here.
# Some Pages URL-normalization modes can canonicalize .html to extensionless paths,
# and combining that with rewrites can create redirect loops.
/sign-in  /login.html    302
/sign-up  /register.html 302
